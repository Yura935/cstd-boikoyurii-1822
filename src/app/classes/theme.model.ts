export class Theme {
    constructor(
        public isDark: boolean,
        public mainColor: string,
        public mainHeadColor: string,
        public mainTextColor: string,
        public currentElement: string
    ) { }
}